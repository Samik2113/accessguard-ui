{
  "name": "func-accessguard-dev",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@azure/cosmos": "^3.17.4"
  },
  "scripts": {
    "build": "echo \"API TS->JS build happens in SWA workflow (src/services -> backend/dist)\"",
    "start": "echo \"Use Azure Functions Core Tools if you run locally\""
  }
}